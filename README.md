# Media AI V2
موقع محمد إبراهيم Media AI + لوحة تحكم `/admin`.

## التشغيل
افتح `index.html`.
لوحة التحكم: `admin/index.html`.

## دخول تجريبي
Username: `admin`
Password: `admin123`

## ملاحظة مهمة
هذه V2 Front-end تجريبية: البيانات محفوظة في `localStorage` داخل نفس المتصفح. لذلك لا تعتبر نظام إدارة آمنًا للإنتاج ولا تصلح لتسجيل دخول حقيقي أو تخزين طلبات عامة بين الزوار. للنسخة الإنتاجية نحتاج Backend + Database + Authentication + Storage.
